{% extends 'base.html.twig' %}

{% block title %}Admin Page{% endblock %}
{#{% block jumbotron %}
        {{ include('post/_flash_messages.html.twig') }}
        <section class="jumbotron text-center">
        <div class="container">
            <h1 class="jumbotron-heading">Eterxoz World</h1>
            <p class="lead text-muted">{{ message }}</p>
            <p>
                <a href="#" class="btn btn-primary my-2">Main call to action</a>
                <a href="#" class="btn btn-secondary my-2">Secondary action</a>
            </p>
        </div>
        </section>{% endblock %} #}


{% block body %}
        <div class="row">
            {% for post in posts %}
                <div class="col-md-4">
                    <div class="card mb-4 shadow-sm">
                        <img class="card-img-top" data-src="holder.js/100px225?theme=thumb&bg=55595c&fg=eceeef&text={{ post.postTitle }}" alt="Card image cap">
                        <div class="card-body">
                            <p class="card-text">{{ post.postBody }}</p>
                            <div class="d-flex justify-content-between align-items-center">
                                <div class="btn-group">
                                    <a href="{{ path('post_show', {'id': post.id}) }}" class="btn btn-outline-success">View</a>
                                    <a href="{{ path('post_edit', {'id': post.id}) }}" class="btn btn-outline-danger">Edit</a>
                                    {{ include('admin/_delete_form.html.twig') }}
                                    <a href="{{ path('add_tags', {'id': post.id}) }}" class ="btn btn-outline-success">Add Tag</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>


            {% endfor %}

        </div>



    <a class="btn btn-outline-dark" href="{{ path('post_new') }}">Create new</a>
{% endblock %}
